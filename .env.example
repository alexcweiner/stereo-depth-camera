VIAM_CHANNEL=stable
VIAM_PORT=8080
BRIDGE_PORT=8081
# Headless USB (default). Set to 1 only for browser WebRTC capture.
STEREO_DEPTH_WEBRTC=0
# Linux/Pi device path for compose.linux.yaml
CAMERA_DEVICE=/dev/video0
