# Linux / Pi: pass the GXIVISION UVC device into viam-server's stock webcam.
services:
  viam-server:
    devices:
      - "${CAMERA_DEVICE:-/dev/video0}:${CAMERA_DEVICE:-/dev/video0}"
    group_add:
      - video
